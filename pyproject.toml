[project]
name = "linear-algebra"
version = "0.1.0"
description = "Linear algebra experiments (numpy, matplotlib, plotly, dash)"
requires-python = ">=3.13"
dependencies = [
    "dash>=4.1.0",
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
    "plotly>=6.7.0",
]
